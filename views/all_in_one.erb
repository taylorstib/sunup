<table>
  <tbody class="zebra">
    <tr>
      <th>Events</th>
      <th>Date</th>
      <th>Difference</th>
      <th># of Fridays</th>
    </tr>
    <tr>
      <td>Today</td>
      <td><%= @today.strftime('%a, %b %d, %Y') unless @today.nil? %></td>
      <td>0</td>
    </tr>
    <% @dates.each do |date| %>
      <tr>
        <td><strong><%= date[:event] %></strong></td>
        <td><%= date[:date].strftime('%a, %b %d, %Y')  %></td>
        <td><%= day_difference_to_words((date[:date] - @today).to_i) %></td>
        <td><%= count_fridays(@today, date[:date]) %></td>
      </tr>
    <% end %>
